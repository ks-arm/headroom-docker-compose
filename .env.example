# Headroom environment template — copy to .env and fill in.
#   cp .env.example .env
#
# ZAI_API_KEY is OPTIONAL. Set it only to route through the z.ai upstream.
# For other providers, add their keys to docker-compose.yml instead
# (ANTHROPIC_API_KEY / OPENAI_API_KEY) and leave this commented.

# ZAI_API_KEY=
